apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: msa-tls
  namespace: msa
spec:
  secretName: msa-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - api.example.com  # ← 실제 도메인으로 교체
